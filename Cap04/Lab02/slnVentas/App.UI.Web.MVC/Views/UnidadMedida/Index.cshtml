@model IEnumerable<App.Entities.Base.UnidadMedida>

@{
    ViewBag.Title = "Lista de Unidad Medida";
}

<h2>Lista de Unidad Medida</h2>

@*Sin usar HTML Helper*@
@*<a href="/Categoria/Create" class="btn btn-success">Nueva Categoría</a>*@
@*Usando HTML helper*@
@Html.ActionLink("Nueva Unidad Medida", "Create", "UnidadMedida", new {@class = "btn btn-success" })
<br /><br />
<table class="table table-striped">
    <tr>
        <th>Nombre</th>
        <th>#</th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Nombre</td>
            <td>
                <a href="@Url.Action("Edit", new { id = item.UnidadMedidaID})">
                    <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
                </a>
            </td>
        </tr>
    }
</table>

