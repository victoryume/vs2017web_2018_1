@{
    ViewBag.Title = "Listado del Unidades de Medida";
}

<h2>Lista de Marcas</h2>

<div class="row">
    <div class="col-sm-1">
        <button onclick="app.helpers.showModal('UnidadMedidaCreatePopup','@Url.Action("create")')" class="btn btn-success">Nuevo</button>
    </div>

    @using (Ajax.BeginForm("Buscar", "UnidadMedida",
        new AjaxOptions
        {
            HttpMethod = "POST",
            InsertionMode = InsertionMode.Replace,
            UpdateTargetId = "listado"
        }))
    {
        <div class="row">
            <div class="form-group col-sm-3 ">
                <input name="filterByName" id="filterByName" class="form-control" placeholder="Buscar por nombre" />
            </div>
            <div class="col-sm-2">
                <button type="submit" class="buscar btn btn-facebook">Buscar</button>
            </div>
        </div>
    }
</div>

<div id="listado">
    @Html.Action("Buscar")
</div>

@Modal.CrearModal("Crear Categoría", "UnidadMedidaCreatePopup")

@section scriptsAdicionales
{
    @Scripts.Render("~/bundles/jqueryval")
}
